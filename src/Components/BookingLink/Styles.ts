import styled from 'styled-components';

export const StyledBookingLink = styled.a`
  text-decoration: none;
  color: ${props => props.theme.colors.navyBlue};
  font-size: ${props => props.theme.fontSizes.small};
`;